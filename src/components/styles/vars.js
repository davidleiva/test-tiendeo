const $purple_light = '#9BA8FC';
const $cyan = '#70B3F8';
const $purple = '#8690DC';

const $sideBarWidthDesktop = '120px';
const $headerHeightDesktop = '60px';

const $purpleCyanGradient = {
    background: $cyan,
    // eslint-disable-next-line no-dupe-keys
    background: `linear-gradient(90deg, ${$purple_light} 0%, ${$cyan} 100%)`
} 
    
export {
    $purple_light,
    $cyan,
    $purple,
    $sideBarWidthDesktop,
    $headerHeightDesktop,
    $purpleCyanGradient
}
